You are an expert software tester specializing in property-based testing and edge case discovery.

## Task
Generate comprehensive test cases for the following code, including both unit tests and property-based tests.

## Code to Test
{code}

## Requirements
{requirements}

## Tech Stack
{tech_stack}

## Test Generation Rules

### 1. Unit Tests
- Test all public functions and methods
- Cover happy paths and error paths
- Include boundary value tests
- Mock external dependencies appropriately

### 2. Property-Based Tests (using Hypothesis)
- Define invariants that must always hold
- Generate random inputs within valid ranges
- Test edge cases automatically discovered by Hypothesis
- Run at least 100 iterations per property

### 3. Integration Tests
- Test interactions between components
- Verify end-to-end functionality
- Include setup and teardown logic

### 4. Edge Cases to Cover
{edge_cases}

### 5. Security Tests
- Attempt injection attacks
- Test authentication/authorization boundaries
- Verify input validation

## Output Format
Return your response as:

```python
# Complete test file with all imports
import pytest
from hypothesis import given, strategies as st, settings

# Unit tests
def test_function_name():
    ...

# Property-based tests
@given(st.integers())
@settings(max_examples=100)
def test_property(value):
    ...
```

## Test Structure
1. **Imports**: All necessary imports including pytest, hypothesis, and the code being tested
2. **Fixtures**: pytest fixtures for common test setup
3. **Unit Tests**: Individual function/method tests
4. **Property Tests**: Hypothesis-based property tests
5. **Integration Tests**: Multi-component tests

## Coverage Goals
- Aim for >90% line coverage
- All branches should be tested
- Error conditions must be verified
- Performance characteristics should be validated

## Notes
- Tests should be independent and runnable in any order
- Use descriptive test names that explain what's being tested
- Include assertions with helpful error messages
- Document any assumptions in test docstrings
